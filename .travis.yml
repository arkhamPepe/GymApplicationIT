language: android
android:
  components:
    - tools
    - platform-tools
    - build-tools-27.0.3
    - android-28
before_install:
  - cd GymCompanion
before_script:
  - chmod +x gradlew
script:
   - ./gradlew build
